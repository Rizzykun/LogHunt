# Keep LF everywhere. The sample logs are generated byte for byte by
# tools/generate_scenario.py, and tests assert that reproducibility - a CRLF
# conversion on checkout would break it on Windows.
* text=auto eol=lf

*.png binary
